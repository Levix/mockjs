export const MAX_NATURE_NUMBER = 9007199254740992;

export const MIN_NATURE_NUMBER = -9007199254740992;
export const CHAR_REGS = {
  lower: /^[a-z]$/,
  upper: /^[A-Z]$/,
  number: /^\d$/,
  symbol: /^\W$/,
  alpha: /^[a-z]$/i,
};
