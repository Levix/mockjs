const defaultResponse = {
  code: 0,
  message: 'success',
  data: ''
}

export function mock(options: any) {

  // 处理空值
  if (!options) {
    return defaultResponse;
  }

  // 处理基本数据

  // 处理Object类型

  // 处理大数据

  return 'mock';
}