{
    "name": "@zsw/mockjs",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "build": "rm -rf dist && tsup --config tsup.config.js",
        "test": "vitest",
        "coverage": "vitest run --coverage",
        "docs": "rm -rf docs/typedoc && npx typedoc src/index.ts --out docs/typedoc"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "typedoc": "^0.24.7"
    }
}
