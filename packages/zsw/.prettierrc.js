module.exports = {
    // 使用空格
    useTabs: false,
    // 使用 4 个空格缩进
    tabWidth: 4,
    // 单行最长字符数量
    printWidth: 125,
    // 使用单引号
    singleQuote: true,
    // 行末需要逗号
    trailingComma: 'all',
    // 行末需要分号
    semi: true,
    // 箭头函数只有一个参数时不需要要括号
    arrowParens: 'avoid',
    // 大括号的首尾需要空格
    bracketSpacing: true,
    // 使用默认的折行标准
    proseWrap: 'preserve',
    // 根据显示样式决定 html 要不要折行
    htmlWhitespaceSensitivity: 'css',
    // 行末尾使用 lf
    endOfLine: 'lf',
    // 格式化嵌入的内容
    embeddedLanguageFormatting: 'auto',
};
