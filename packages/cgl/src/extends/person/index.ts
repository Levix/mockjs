import { getId } from './id';
import { getPhone } from './phone';
import { getFirstName, getLastName, getName, getCFirstName, getCLastName, getCName } from './name';

export default {
    id: getId,
    phone: getPhone,
    firstName: getFirstName,
    lastName: getLastName,
    name: getName,
    cFirstName: getCFirstName,
    cLastName: getCLastName,
    cName: getCName,
}
